/*
    Header files can be used to extract function declerations 
    and allow imports. 
    Function declarations are important since they prevent compiler
    to make assumptions about eturn types, etc. 
*/

void encrypt(char * message);